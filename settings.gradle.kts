rootProject.name = "AwesomeArmorStandEditor"

// Standalone plugin — no composite build. LycoLib is an optional runtime soft-integration
// reached via reflection (see integration/LycoLibHook.kt), never a compile/build dependency.
